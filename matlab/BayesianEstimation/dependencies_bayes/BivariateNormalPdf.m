function thePdf = BivariateNormalPdf(x,u,K)% thePdf = BivariateNormalPdf(x,u,K)%% Return probability of a bivariate normal% over the range passed.  Does not check% for singular covariance matrix.%% 7/2/98  dhb  Added comment.Kinv = inv(K);Kdet = det(K);nPoints = size(x,2);thePdf = zeros(1,nPoints);for i = 1:nPoints	thePdf(i) =  exp( -0.5*(x(:,i)-u)'*Kinv*(x(:,i)-u));endthePdf = (1/(2*pi*Kdet))*thePdf;